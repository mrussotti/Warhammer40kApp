// componenets/nextPhaseButton.js
import React from 'react';
import { Button } from 'react-native';

const NextPhaseButton = ({ onPress }) => {
  return <Button title="Next Phase" onPress={onPress} />;
};

export default NextPhaseButton;
